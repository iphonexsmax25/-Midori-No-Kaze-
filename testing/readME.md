This directory ("testing") is used to understand better about the function used.

index.html serves as the interface between the end-user and the back-end functionality.

interface.py is a potential main function, which when executed will run the entire program.

# Discovery
What we found out is that webbrowser and Flask is useful in combining Python with HTML. I think webbrowser is more useful than Flask since webbrowser can open the html file directly, while Flask require you to click a link.